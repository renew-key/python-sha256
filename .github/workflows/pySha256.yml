name: pytest
on:
  push:
    branches:
      - main
  workflow_dispatch:
  pull_request:
    types: [opened]

jobs:
  pysha256:
    runs-on: windows-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: install pyinstaller
        run: pip install pyinstaller
      - name: package sha256 and run sha256
        run: |
          pyinstaller.exe .\sha256.py -F
          cd .\dist\
          .\sha256.exe .\sha256.exe
